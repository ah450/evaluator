<project>
  <target name="clean">
    <delete dir="<%= @build_directory %>"/>
    <delete dir="<%= @src_directory %>" />
    <delete dir="<%= @tests_directory %>" />
  </target>

  <target name="compile_src">
      <mkdir dir="<%= @build_directory %>/classes"/>
      <javac includeantruntime="true" failonerror="true"
      srcdir="<%= @src_directory %>" destdir="<%= @build_directory %>/classes"/>
  </target>

  <target name="compile_tests">
    <javac includeantruntime="true" srcdir="<%= @tests_directory %>"
    destdir="<%= @build_directory %>/classes"
    />
  </target>

  
  <target name="delete_test_source">
    <delete dir="<%= @tests_directory %>" />
  </target>

  <target name="clear_test_results">
    <delete dir="<%= @build_directory %>/tests"/>
  </target>

  <target name="test">
    <mkdir dir="<%= @build_directory %>/tests" />
    <junit includeantruntime="true" haltonerror="no" haltonfailure="no" fork="no" dir="<%= @src_directory %>">
              <classpath>
                      <pathelement location="lib/hamcrest-junit-2.0.0.0.jar" />
                      <pathelement location="lib/java-hamcrest-2.0.0.0.jar" />
                      <pathelement location="<%= @build_directory %>/classes"/>
              </classpath>
              <formatter type="xml"/>
            <batchtest todir="<%= @build_directory %>/tests" fork="no">
              <fileset dir="<%= @build_directory %>/classes" includes="**/*Test.class" />
            </batchtest>
      </junit>
    </target>
</project>